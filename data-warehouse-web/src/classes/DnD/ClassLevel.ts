export class ClassLevel {
    className: string = "";
    level: number = 1;

    constructor(className: string, level: number) {
        this.className = className;
        this.level = level;
    }
}